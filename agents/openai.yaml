interface:
  display_name: "Skill Hook Authoring"
  short_description: "Create shared agent skills, hooks, plugins, and extensions without drift."
  default_prompt: "Create or update a repo-owned skill, hook, plugin, or extension package from official docs and install derived copies without editing them directly."
